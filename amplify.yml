version: 1
applications:
  - frontend:
      phases:
        preBuild:
          commands:
            - npm ci
        build:
          commands:
            - npm run build -- --configuration production --base-href=/
      artifacts:
        baseDirectory: dist/dashboard/browser
        files:
          - '**/*'
      cache:
        paths:
          - node_modules/**/*
