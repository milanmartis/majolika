version: 1
applications:
  - frontend:
      phases:
        preBuild:
          commands:
            - npm ci
        build:
          commands:
            - npm run build:seo
      artifacts:
        baseDirectory: dist/dashboard/browser
        files:
          - '**/*'
      cache:
        paths:
          - node_modules/**/*
